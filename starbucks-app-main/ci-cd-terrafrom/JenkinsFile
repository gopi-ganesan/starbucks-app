pipeline {
    agent any

    parameters {
        string(
            name: 'environment',
            defaultValue: 'dev',
            description: 'Environment to deploy (dev, staging, prod)'
        )
        choice(
            name: 'terraform_action',
            choices: ['plan', 'apply', 'destroy'],
            description: 'Terraform action'
        )
    }

    stages {

        stage('Preparing') {
            steps {
                sh 'echo preparing'
            }
        }

        stage('Git Checkout') {
            steps {
                git(
                    url: 'https://github.com/gopi-ganesan/starbucks-app.git',
                    branch: 'main',
                    credentialsId: 'github-creds'   
                )
            }
        }

        stage('Debug Workspace') {
            steps {
                sh '''
                    echo "Workspace: $(pwd)"
                    ls -la
                    ls -la ci-cd-terrafrom/ || echo "ci-cd-terrafrom NOT FOUND"
                    ls -la ci-cd-terrafrom/eks/ || echo "eks folder NOT FOUND"
                    find . -name "*.tf" 2>/dev/null | head -20
                '''
            }
        }

        stage('Terraform Init') {
            steps {
                withCredentials([[
                    $class: 'AmazonWebServicesCredentialsBinding',
                    credentialsId: 'aws-credentials'
                ]]) {
                    sh '''
                        export AWS_DEFAULT_REGION=us-east-1
                        terraform -chdir=ci-cd-terrafrom/eks init  
                    '''
                }
            }
        }

        stage('Terraform Validate') {
            steps {
                withAWS(credentials: 'aws-credentials', region: 'us-east-1') {
                    sh '''
                        terraform -chdir=ci-cd-terrafrom/eks validate 
                    '''
                }
            }
        }

        stage('Terraform Action') {
            steps {
                withAWS(credentials: 'aws-credentials', region: 'us-east-1') {
                    script {
                        if (params.terraform_action == 'plan') {
                            sh """
                                terraform -chdir=ci-cd-terrafrom/eks plan \
                                  -var='environment=${params.environment}' \
                                  -out=tfplan
                            """
                        }
                        else if (params.terraform_action == 'apply') {
                            sh """
                                terraform -chdir=ci-cd-terrafrom/eks apply \
                                  -var='environment=${params.environment}' \
                                  -auto-approve
                            """
                        }
                        else if (params.terraform_action == 'destroy') {
                            sh """
                                terraform -chdir=ci-cd-terrafrom/eks destroy \
                                  -var='environment=${params.environment}' \
                                  -auto-approve
                            """
                        }
                    }
                }
            }
        }
    }

    post {
        success {
            echo 'Pipeline executed successfully! '
        }
        failure {
            echo 'Pipeline failed! '
        }
    }
}